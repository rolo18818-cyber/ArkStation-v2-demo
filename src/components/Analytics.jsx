import AdvancedAnalytics from './AdvancedAnalytics'

export default function Analytics({ theme, currentUser, userRole }) {
  return <AdvancedAnalytics theme={theme} currentUser={currentUser} userRole={userRole} />
}
